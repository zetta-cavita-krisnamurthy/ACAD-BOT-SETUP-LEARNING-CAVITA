const typeDef = require('./for_test.typedef');
const resolvers = require('./for_test.resolvers');
const ForTestModel = require('./for_test.model');

module.exports = {
  typeDef,
  resolvers,
  ForTestModel,
};
